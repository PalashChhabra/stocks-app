const baseAPIurl = "http://131.181.190.87:3001";

const externalurls = {
  allstocksDataAPI: `${baseAPIurl}/all`,
  filteredIndustryData: `${baseAPIurl}/industry`,
  getstockHistory: `${baseAPIurl}/history`,
};

export default externalurls;
